
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JRLibManagement</title>
      <%= stylesheet_link_tag 'common', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css">
    <link rel="stylesheet" href="style.css">

    <style>
        body, html {
            height: 100%;
            margin: 0;
            padding: 0;
            background: url('https://wallpaper-house.com/data/out/8/wallpaper2you_231925.jpg') center center fixed;
            background-size: cover;
            font-family: 'Montserrat', sans-serif;
        }

    </style>
</head>
<body>
    <div class="wrapper">
        <div class="user-card">
            <div class="user-card-img">
                <div class="initials"><%= image_tag @book.image, style: 'width: auto;', loading: 'lazy' if @book.image.present? %></div>
            </div>
            <div class="user-card-info">
                <div class="profile-header">
                    <h2>Book Detail</h2>
                    <% if current_user && current_user.admin? %>
                        <p class="edit-link">
                            <a href="<%= edit_book_path(@book) %>">
                                <i class="fas fa-edit"></i> Edit
                            </a>
                        </p>
                    <% end %>
                </div>
                <table>
                    <tr>
                        <th class="cofd">Author</th>
                        <td class="cofd"><%= @book.author_name %></td>
                    </tr>
                    <tr>
                        <th class="cofd">ISBN</th>
                        <td class="cofd"><%= @book.isbn %></td>
                    </tr>
                    <tr>
                        <th class="cofd">Genre</th>
                        <td class="cofd"><%= @book.genre %></td>
                    </tr>
                    <tr>
                        <th class="cofd" >Number of Books</th>
                        <td class="cofd"><%= @book.number_of_books %></td>
                    </tr>
                    <tr>
                        <th class="cofd">Published Date</th>
                        <td class="cofd"><%= @book.published_year %></td>
                    </tr>
                </table>
                                      <% if current_user && current_user.admin? %>
                        <tr>
                            <td colspan="2">
                                <%= link_to 'Back', books_path, class: 'btn-back' %>
                            </td>
                        </tr>
                    <% else %>
                        <tr>
                            <td colspan="2">
                                <%# <%= link_to 'Borrow', borrow_book_path(@book), class: 'btn-borrow' %> %>
                            </td>
                        </tr>
                    <% end %>
            </div>
        </div>
    </div>
</body>
</html>
